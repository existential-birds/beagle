# Logs
logs/
*.log

# OS files
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Node (if used for MCP servers)
node_modules/
npm-debug.log*

# Python (if used for MCP servers)
__pycache__/
*.py[cod]
.env
.venv/
venv/

# Publishing
publish-*.sh
.publish-*-done
.publish-done

# Build outputs
dist/
build/

# Security - prevent accidental commits
*.pem
*.key
*.p12
*.pfx
*credentials*
*secret*
.env*
!.env.example
.serena/
.claude/worktrees/

.planning/
.beagle/
skill_research/

# Review tooling
.daydream/
.review-output.md

# Local agent-harness settings
.claude/settings.local.json

# Entire session recording — personal tooling, not a repo dependency.
# Keep the harness integrations local so cloning the repo does not opt a
# contributor into session capture. .codex/INSTALL.md is Beagle's own Codex
# support doc and stays tracked — only the Entire-generated files are ignored.
.entire/
.codex/config.toml
.codex/hooks.json
.cursor/
.opencode/
.pi/
