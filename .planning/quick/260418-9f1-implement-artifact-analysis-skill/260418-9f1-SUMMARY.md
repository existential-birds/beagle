---
quick_id: 260418-9f1
description: implement the new skill as described in .beagle/concepts/artifact-analysis/spec.md
date: 2026-04-18
status: complete
commits:
  - 499a97c
  - 63fc4b5
---

# Quick Task 260418-9f1: artifact-analysis skill

## What shipped

- `plugins/beagle-analysis/skills/artifact-analysis/SKILL.md` — 218 lines, dual-mode (no `disable-model-invocation`), 15 sections covering purpose, trigger rules, workflow, inputs, output location, scan plan, subagent dispatch, skim strategies, skip patterns, citations, synthesis, failure modes, budget defaults, companion contract, tone, out-of-scope, and references.
- `plugins/beagle-analysis/skills/artifact-analysis/references/subagent-brief.md` — mechanical template for per-slice briefs with required frontmatter (`status`, `slice`, `brief_hash`, `paths_read`, `paths_skimmed`, `paths_skipped`) and partial-failure protocol.
- `references/citation-schema.md` — required fields (`path`, `excerpt`), optional fields (`lines`, `heading`, `document_type`), footnote convention, and incomplete-metadata policy.
- `references/report-template.md` — literal skeleton for the seven fixed sections in spec-mandated order.
- `references/failure-modes.md` — partial-success, empty-corpus (returns cleanly, not an error), silent-failure detection, re-run protection, verification checklist.
- `references/companion-contract.md` — input semantics, return shapes, worked examples for `prfaq-beagle`, `brainstorm-beagle`, `strategy-interview`, and standalone user.
- `references/skip-patterns.md` — default denylist (sensitive, binary/media, vendor/build) with override rules.
- `CHANGELOG.md` [Unreleased] entry.
- `CLAUDE.md` Key Skills table row.

## Open Questions resolved during planning

1. **Slug rule** — intent-based (when present) or first-path-basename (when absent); prefixed `YYYY-MM-DD-`; 60-char word-boundary truncation — identical to web-research.
2. **Skip denylist** — concrete glob list finalized in `references/skip-patterns.md`. First version reads plain text/markdown only.
3. **Silent-failure detection** — stub-file rule identical to web-research; absence = silent failure.
4. **Re-run protection** — refuse by default; `refresh: true` archives prior run into `.archive-<timestamp>/`.
5. **Trigger calibration** — local-doc intent keywords only; explicit negative examples for codebase / web / llm-judge / humanize.
6. **`plan_gate` parameter** — deferred per spec guidance "add only when needed".

## Structural divergences from web-research (intentional)

- **No plan review gate.** Local scanning is cheap; `plan.md` is audit-only.
- **No fail-fast on missing tools.** Filesystem tooling is assumed present; absence surfaces as per-subagent failure.
- **Optional intent parameter** with generic-salient-extraction fallback (vs. web-research's required research question).
- **Seven fixed report sections** instead of four (Documents Found / Key Insights / User & Market / Technical / Ideas & Decisions / Raw Detail / Gaps & Limitations).
- **Empty-corpus returns cleanly** instead of erroring — callers detect via the Gaps & Limitations section.
- **Extra reference file** — `skip-patterns.md` (no web-research equivalent needed).

## Verification

- `wc -l SKILL.md` → 218 (under 500 ceiling).
- `grep disable-model-invocation SKILL.md` → no matches (dual-mode confirmed).
- All six fixed report section names present in `SKILL.md` and `references/report-template.md`.
- All six reference files linked from `SKILL.md` by relative path.
- Cross-links verified: `subagent-brief.md` → `skip-patterns.md` + `citation-schema.md`; `report-template.md` → `citation-schema.md`; `failure-modes.md` stands alone; `companion-contract.md` → `failure-modes.md`.
- `grep artifact-analysis CHANGELOG.md CLAUDE.md` → one hit each.

## Commits

- `499a97c` — feat(beagle-analysis): add artifact-analysis skill
- `63fc4b5` — docs: register artifact-analysis skill in CHANGELOG and CLAUDE.md
